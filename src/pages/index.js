import Header from "../components/Header";

const Home = () => {
    return (
        <div>
            <Header />
            <h1>Welcome to the SSR Next.js App</h1>
        </div>
    );
};

export default Home;
