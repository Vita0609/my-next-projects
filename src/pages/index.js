import Header from "../components/Header";

const Home = () => {
    return (
        <div>
            <Header />
            <h1>Welcome to the SSR</h1>
        </div>
    );
};

export default Home;
